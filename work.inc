<?php

/**
 * @file
 * Contains the work callback to operate on the entity. The entity should not
 * be saved at this
 */

namespace Drupal\_drush_script;

use EntityMetadataWrapper;

/**
 * Do the work on the wrapper.
 */
function do_work(EntityMetadataWrapper $wrapper, array $vars = array()) {
  // Do the work.
}
